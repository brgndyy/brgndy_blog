import Container from '../_composables/container/Container';
import { descriptionContainer } from './postDescription.css';

export default function PostDescription() {
  return <Container className={descriptionContainer}>PostDescription</Container>;
}
